# source opt setup bash
source /opt/ros/humble/setup.bash

# colcon build

# create src folder 
mkdir -p ~/ros2_ws/src
cd ~/ros2_ws/src

# create package
ros2 pkg create --build-type ament_python classroom_system

# Step 6 — Activate your workspace
source ~/ros2_ws/install/setup.bash

# ros request
ros2 service call /add_two_ints example_interfaces/srv/AddTwoInts "{a: 10, b: 32}"